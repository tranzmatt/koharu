# Agent resources

This directory contains project-specific resources for coding agents.

- `skills/` contains reusable workflows for specialized maintenance tasks.
- Repository-wide instructions remain in [`AGENTS.md`](../AGENTS.md), where Codex can discover them automatically.
