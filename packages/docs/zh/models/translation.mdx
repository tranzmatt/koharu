---
title: '配置翻译'
description: '设置语言、指令、量化、采样和图像输入。'
icon: 'language'
---

在处理选择器或**设置 → 翻译**中设置输出。运行开始时会捕获配置，后续修改应用于下一次运行。服务商连接和凭据则在每次请求时读取。

## 语言与指令

选择目标语言，并为名字、术语、语气、代词、格式或无需翻译的内容提供简短指令。指令随 Koharu 设置保存并应用于所有项目，没有按项目设置的字段。在多页上测试，先修正 OCR。

LFM 2.5 和 Ministral 3 只支持有限的目标语言，目标语言不受支持时运行会失败。其他本地模型接受 Koharu 提供的所有目标语言。

## 本地量化

GGUF 可能提供多个量化版本。较小文件节省下载空间与内存，但可能损失质量；较大文件可能更慢或超出内存。托管服务自行控制推理精度。

## 生成参数

| 控制项                              | 调整内容                     |
| ----------------------------------- | ---------------------------- |
| temperature、top P、top K、min P    | 采样与随机性                 |
| repeat、frequency、presence penalty | 重复行为                     |
| 最大输出 token                      | 响应长度预算                 |
| 推理                                | 支持模型的额外推理           |
| 视觉                                | 是否向支持模型发送源页面图片 |

<Tip>从服务商默认值开始，每次只改一个变量。较高随机性容易降低名字和术语的一致性。</Tip>

推理可能增加延迟与 token 使用。模型不支持推理或视觉时，这两项会被静默去掉；其他不受支持的选项可能被服务商或模型忽略或拒绝。

## 文本与视觉输入

纯文本请求发送语义原文。启用**视觉**后，支持的模型还会收到源页面图片。自定义 OpenAI 兼容服务默认视为支持图片，若其不支持图像消息，请关闭**视觉**。

生成结果仍是可编辑的项目数据。导出前请[检查译文与布局](/zh/guides/review)。
