{
  "name": "@koharu/bridge",
  "version": "0.1.0",
  "private": true,
  "license": "MIT OR Apache-2.0",
  "type": "module",
  "exports": {
    "./canvas": "./src/canvas.ts",
    "./protocol": "./src/protocol.ts"
  },
  "scripts": {
    "lint": "oxlint src --ignore-pattern src/protocol.ts --ignore-pattern src/wasm",
    "typecheck": "tsc --noEmit",
    "dev": "nodemon --watch $(npm prefix)/crates/koharu-canvas --watch $(npm prefix)/crates/koharu-rasterizer --ext rs,toml --delay 250ms --on-change-only --exec bun -- run --filter @koharu/bridge build:dev",
    "build": "bun x wasm-pack@0.15.0 build $(npm prefix)/crates/koharu-canvas --release --target web --out-dir $(npm prefix)/packages/bridge/src/wasm --out-name koharu_canvas --no-pack",
    "build:dev": "bun x wasm-pack@0.15.0 build $(npm prefix)/crates/koharu-canvas --dev --target web --out-dir $(npm prefix)/packages/bridge/src/wasm --out-name koharu_canvas --no-pack"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "oxlint": "^1.80.0",
    "typescript": "^7.0.2"
  }
}
