# Koharu Metrics

`koharu-metrics` converts `tracing` spans and events into anonymous product
analytics. Application code records native outcomes; the crate owns identity,
shared context, queuing, and transport.
